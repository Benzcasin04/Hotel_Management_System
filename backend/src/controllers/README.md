# Controllers

Controllers handle the request/response logic.
Each controller should correspond to a specific resource (auth, rooms, bookings, etc.)

Example controllers to add:
- auth.controller.ts - Authentication (login, register, logout)
- room.controller.ts - Room CRUD operations
- booking.controller.ts - Booking management
- user.controller.ts - User management
